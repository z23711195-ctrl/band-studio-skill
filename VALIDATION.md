# 验证范围

## v0.2.0 · Jev 接入

核验日期：2026-09-23。

- Skill Creator 的 `quick_validate.py` 通过；Python 编译与差异格式检查通过。
- **73 项离线测试通过**：原有 41 项加 Jev 的 32 项。覆盖三种模式、默认零网络、无密钥降级、配置优先级、输出与原件保护、缓存命中/失效/过期、HTTP 错误与重定向禁止、超时及异常响应。
- 独立前向试用：三份合成示例各执行 dry-run 和无凭据 `--execute`，共 6 个隔离 CLI 运行；前者返回 `dry_run`，后者返回 `deferred / missing_api_key`，全部零网络调用，输入未改写。
- **真实 Jev API 调用 7 次**，仅发送合成文字；返回实际模型 `jev-1.13.0`。合计输入 10,693 tokens、输出 1,040 tokens。测试期间单次适配器报告耗时约 0.96–1.76 秒；不是端到端工作流提速基准，也不承诺其他网络或输入规模有相同速度。
- 参考排序把匹配 Dub/Sax 目标的示例排在 EDM/Dubstep 示例之前，并保留依据不足的复核提示；仅有提示词而没有核听，不能支持“已确认听到 Sax”的结论。
- Suno 文字检查区分了排除标签和正向风格，能指出实际字段中 `source_attached=false` 的冲突；观察字段缺失时返回未知，没有拿计划字段填补。
- 真实试用发现一个问题模板会让 `source_attached=true` 的文字一致性检查产生额外疑虑。模板修正为清楚区分“按已提供字段判断”与“独立验证服务端”；`p2` 复测确认：缺来源仍标冲突，字段齐备时返回 `no_text_conflicts_detected`，始终保留 `advisory_only`。
- 相同输入二次调用实际命中缓存：`network_calls=0`，本次 API input/output tokens 均为 0，历史用量另存 `stored_usage`。模板变化使旧缓存失效。

上述真实请求不是听音、看图或 Suno 生成测试，不代表所有作品的风格判断准确率。Jev 只辅助排序与文本核对，媒体验收仍由原流程执行。公开包只含合成示例，不含 API key、私有配置、乐队媒体或完整词稿。

## v0.1.0

- Skill Creator 的 quick_validate 检查通过。
- Python 标准库单元测试：41 项通过，覆盖文件索引、重复组、原件保护、工具选择、子进程失败与超时、SRT 时码和视频尺寸/时长。
- 独立前向试用：两份相同合成 WAV、损坏 MP4、文本、隐藏项和符号链接；重复组正确，损坏项留在报告，原文件未改动，没有上传或播放。
- 前向试用发现 catalog 未读取环境变量，已修复 CLI/函数的工具选择优先级并加入回归。
- macOS 上使用 FFmpeg 9.0.1 / FFprobe 9.0.1 做真实合成检验：3 秒、320×180、25 fps、H.264/AAC，烧录中文 SRT 后实际看帧；doctor 和 verify_delivery 返回成功。
- 超出视频时长的 SRT、错误目标分辨率均实际被拒绝，原合成文件校验值不变。

本节 v0.1.0 测试使用临时、模拟或合成数据。没有用真实乐队素材做公开仓库测试，也没有用本包测试 Suno 实际生成、云音频分析、账号登录或逐音准确率。

运行：`python3 -m unittest discover -s tests -v`。GitHub Actions 重跑单元测试和 Python 编译检查；真实媒体/视觉检查是单独的本地验证，不伪称为 CI 的覆盖范围。
